# PyQt6 계산기 GUI 설정 가이드

## 📋 개요

PyQt6를 사용하여 Windows 계산기와 유사한 GUI 인터페이스를 구현했습니다.
Core 계산 로직(`ArithmeticOperations`)과 완전히 분리된 GUI 레이어로 구성되어 있습니다.

---

## 🚀 설치 및 실행

### 1. PyQt6 설치

```bash
pip install -r requirements.txt
```

또는 직접 설치:

```bash
pip install PyQt6>=6.6.0
```

### 2. GUI 실행

```bash
python -m src.gui.calculator_gui
```

또는:

```bash
python src/gui/calculator_gui.py
```

---

## 🎨 GUI 기능

### 기본 기능

1. **숫자 입력**: 0-9 버튼
2. **사칙연산**: +, -, ×, ÷
3. **소수점**: . 버튼
4. **계산**: = 버튼 (파란색 강조)
5. **부호 변경**: +/- 버튼

### 고급 기능

1. **백분율**: % 버튼
2. **역수**: 1/x 버튼
3. **제곱**: x² 버튼
4. **제곱근**: ²√x 버튼

### Clear 기능

1. **C (Clear)**: 전체 초기화
2. **CE (Clear Entry)**: 현재 입력만 초기화
3. **⌫ (Backspace)**: 마지막 자리 삭제

### 메모리 기능

1. **MC (Memory Clear)**: 메모리 초기화
2. **MR (Memory Recall)**: 메모리 값 불러오기
3. **M+ (Memory Add)**: 현재 값을 메모리에 더하기
4. **M- (Memory Subtract)**: 현재 값을 메모리에서 빼기
5. **MS (Memory Store)**: 현재 값을 메모리에 저장

---

## 🏗️ 아키텍처

### 구조

```
src/
├── arithmetic/              # Core 로직 (순수 함수/클래스)
│   ├── arithmetic_operations.py
│   └── exceptions.py
└── gui/                     # GUI 인터페이스
    └── calculator_gui.py
```

### 설계 원칙

1. **관심사 분리**: Core 로직과 GUI 완전 분리
2. **재사용성**: Core 로직은 CLI, GUI, API 등에서 재사용 가능
3. **의존성 방향**: GUI → Core (단방향 의존성)

---

## 💻 사용 방법

### 기본 계산

1. 숫자 버튼 클릭하여 첫 번째 숫자 입력
2. 연산자 버튼 클릭 (+, -, ×, ÷)
3. 숫자 버튼 클릭하여 두 번째 숫자 입력
4. = 버튼 클릭하여 결과 확인

### 예시

```
10 + 30 = 40
1. "1" → "0" 클릭 → "10" 표시
2. "+" 클릭
3. "3" → "0" 클릭 → "30" 표시
4. "=" 클릭 → "40" 표시
```

### 고급 연산

- **백분율**: 숫자 입력 → "%" 클릭
- **제곱**: 숫자 입력 → "x²" 클릭
- **제곱근**: 숫자 입력 → "²√x" 클릭
- **역수**: 숫자 입력 → "1/x" 클릭

---

## 🔧 기술 스택

- **PyQt6**: GUI 프레임워크
- **Python 3.10+**: 프로그래밍 언어
- **Core 로직**: `ArithmeticOperations` 클래스 재사용

---

## 📝 주요 특징

1. **Windows 계산기 스타일**: 유사한 레이아웃과 동작
2. **에러 처리**: 0으로 나누기 등 예외 상황 처리
3. **메모리 기능**: 계산 중간값 저장 및 불러오기
4. **고급 연산**: 백분율, 제곱, 제곱근, 역수 지원
5. **사용자 친화적**: 직관적인 버튼 배치 및 색상

---

## 🎯 향후 개선 가능성

1. **계산 이력**: 이전 계산 기록 보기
2. **과학 계산기 모드**: 삼각함수, 로그 등 추가
3. **키보드 입력 지원**: 마우스 없이도 사용 가능
4. **테마 변경**: 다크 모드 등
5. **다국어 지원**: 영어, 일본어 등

---

## ⚠️ 주의사항

- PyQt6는 Python 3.7 이상 필요
- Windows, macOS, Linux 모두 지원
- GUI 실행 시 터미널 창이 함께 열릴 수 있음 (정상 동작)

---

**작성일**: 2025-12-16  
**버전**: v1.0

